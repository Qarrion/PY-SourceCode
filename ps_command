pip list
pip install <package>
pip install <package==x.x.x>
pip uninstall <package>


pip freeze > requirements.txt
pip install -r requirements.txt
pip uninstall -y -r requirements.txt


python -m venv .venv
rm -r .venv

pip install -e LOCAL_LIB
pip uninstall LOCAL_LIB
